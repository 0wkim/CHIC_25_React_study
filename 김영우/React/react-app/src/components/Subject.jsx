// import React from 'react';

function Subject(props) {
    return (
        <>
            <header>
                <h1>{props.title}</h1>
                <p>{props.sub}</p>
            </header>
        </>
    )
}

export default Subject;